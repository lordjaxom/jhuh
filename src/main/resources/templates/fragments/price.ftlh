<div class="label-text">
<#--    <div class="line">${label.vendor}</div>-->
    <div class="line line-name">${label.name}</div>
    <div class="line line-variant">${label.variant}</div>
    <div class="line line-price">${label.price?string.currency}</div>
</div>
